<%= error_messages_for 'article' %>

<div class="merge">
  <h4><%= _("Merge Articles") %></h4>

  <p>Article IDs

  <%= text_field_tag :merge_with %>
  <%= hidden_field_tag("merge[from]", :value => @article.id) %></p>
  <!--<%= text_field(:merge, :from, :size => 6, :value => @article.id) %></p> -->

  <input id='merge_button' type="submit" value="Merge" name="box" />
</div>
